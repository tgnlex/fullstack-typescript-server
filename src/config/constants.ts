const PORT: number = 3000;
const HOST: string = "localhost";
const SERVER: string = `http://${HOST}:${PORT}/`;
const LOGLEVEL: string = 'debug';
const EMAIL: string = 'awdevtestmail@gmail.com';
const VERSION: string = 'v1.0.0';

export {HOST, PORT, SERVER, LOGLEVEL, EMAIL, VERSION}
